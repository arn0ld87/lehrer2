{
  "name": "unterrichtsassistenz-lsa",
  "version": "0.0.0",
  "private": true,
  "description": "Unterrichtsassistenz LSA — Fundament/Planung (noch keine Anwendung). Nur Doku, Governance, CI-Geruest.",
  "type": "module",
  "scripts": {
    "verify:docs": "bash scripts/verify-docs.sh",
    "format": "prettier --write \"**/*.{md,yml,yaml,json}\"",
    "format:check": "prettier --check \"**/*.{md,yml,yaml,json}\""
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "prettier": "^3.3.3"
  }
}
